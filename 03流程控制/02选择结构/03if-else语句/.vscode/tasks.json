{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/03if-else语句.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}