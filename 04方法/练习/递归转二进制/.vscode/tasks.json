{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/递归转二进制.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}