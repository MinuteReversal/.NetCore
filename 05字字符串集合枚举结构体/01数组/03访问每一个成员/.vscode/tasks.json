{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/03访问每一个成员.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}