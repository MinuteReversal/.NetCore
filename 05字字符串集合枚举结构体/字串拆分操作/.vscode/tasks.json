{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/字串拆分操作.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}