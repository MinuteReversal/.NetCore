{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/初始化结构体数组.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}