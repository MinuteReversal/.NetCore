{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/04属性的进化.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}