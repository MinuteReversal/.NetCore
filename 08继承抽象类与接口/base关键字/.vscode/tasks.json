{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/base关键字.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}