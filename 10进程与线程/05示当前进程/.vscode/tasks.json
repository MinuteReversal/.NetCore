{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/04显示当前进程.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}