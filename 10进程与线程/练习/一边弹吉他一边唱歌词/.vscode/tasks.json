{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/一边弹吉他一边唱歌词.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}