{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/向cmd输入命令.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}