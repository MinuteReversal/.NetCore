{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/修改文件名字.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}