{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "build.bat",
      "args": ["\"${workspaceFolder}\\${fileBasenameNoExtension}\""],
      "problemMatcher": ["$msCompile"],
      "group": {
        "isDefault": true,
        "kind": "build"
      }
    }
  ]
}
